# Optional: copy to .env in this directory to change the host port the shared
# proxy listens on (default 80). With a non-80 port, URLs become
# http://admin.kozijnr.localhost:<PROXY_PORT>.
PROXY_PORT=80
